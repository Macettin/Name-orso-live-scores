# Orso Live Scores

A Next.js TypeScript web app for volleyball and basketball tournaments. It uses Tailwind CSS and mock data first, with pages for fixtures, live scoring, standings, teams, player stats, match reports, admin score entry, and QR-code-ready court/hall match pages.

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.
